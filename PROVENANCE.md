# Provenance

Every source is a California Department of Education public file, downloaded from CDE's
public data pages in a browser, the way the pages are meant to be used. The pipeline
therefore treats source files as locally acquired inputs rather than fetching them at
build time: record the access date beside each file when acquired, and the pipeline
stamps it into coverage output. CI never fetches a source file: no step in the
build or the gate reaches CDE. That is narrower than "CI never touches the
network", which is what this line said until 2026-08-29 and which is not true of
`make verify-ci`: `uv sync`, `npm ci`, `pip-audit`, `npm audit` and the pinned
`uvx` runs of semgrep and zizmor all reach a package index or an advisory
database. What never crosses the network is the data.

| # | Source | What it provides | Acquisition | Status |
|---|---|---|---|---|
| D1 | Public Schools and Districts directory (`pubschls.txt`), https://www.cde.ca.gov/schooldirectory/ (download: text report) | Canonical school list: CDS codes, names, districts, status, type, grades, coordinates, charter/virtual flags | Browser download, ~8.8 MB tab-delimited | Endpoint verified live 2026-08-07 (HEAD 200, text/csv, 8,788,547 bytes); parser built |
| D2 | Census Day Enrollment Data, https://www.cde.ca.gov/ds/ad/filesenrcensus.asp (file structure: https://www.cde.ca.gov/ds/ad/fsenrcensus.asp; the pre-2026 URL filesenr.asp now 404s, renamed upstream, observed 2026-08-07) | Enrollment by school, grade, demographic group | Browser download, annual. Acquire from the browser page and record which school year's file was taken | 2025-26 file acquired 2026-08-07, saved as `cdenroll2526.txt` (32,041,859 bytes, 269,090 data rows); parser built; ReportingCategory names reviewed against the file structure page 2026-08-07; the file's district and statewide `AggregateLevel` rows are read for the context columns on each school page, taking only `Charter=ALL` rows, because each entity is published three times (charter, non-charter, both) and in this file Davis Joint Unified's three district rows read 561 / 7,682 / 8,243 for the same category. Homeroom never sums school rows into a district or state figure |
| D3 | Chronic absenteeism files, https://www.cde.ca.gov/ds/ad/filesabd.asp, linking https://www3.cde.ca.gov/demo-downloads/attendance/chronicabsenteeism25-v2.txt (file structure: https://www.cde.ca.gov/ds/ad/fsabd.asp) | Chronic absenteeism eligible cumulative enrollment, count, and rate by school and group, with CDE small-cell masking | Browser download, annual | **Acquired 2026-08-21.** The 2024-25 file (`chronicabsenteeism25.txt`, 33,781,100 bytes, 341,490 data rows) was downloaded and its header read directly: identity columns are spaced (`Academic Year`, `Charter School`, ...) while the three measure columns are concatenated (`ChronicAbsenteeismEligibleCumulativeEnrollment`, `ChronicAbsenteeismCount`, `ChronicAbsenteeismRate`); `Charter School` and `DASS` are two independent `All`/`Yes`/`No` dimensions, so a genuine district or state figure needs both to read `All`, not the one dimension D2 has. 25 reporting-category codes were observed, none shared with D2's own codes for the same underlying groups (D2's `RE_A` is this file's `RA`); see `src/homeroom/profiles.py` `ABSENTEEISM_CATEGORY_NAMES`. Scanning every row found 104,469 of 341,490 (30.6%) carrying a mask on at least one of the three numeric cells, and masking is always all three together, never a subset. `src/homeroom/absenteeism.py` was written and verified against this file; `docs/ROADMAP.md` M3 records the measured coverage. Wired into `make data` and `make site`'s default invocation (Makefile `--absenteeism`), rendered on every school page (ROADMAP M3) |
| D4 | CA School Dashboard research files, indexed at https://www.cde.ca.gov/ds/ad/accdf.asp, one download page and one record layout per indicator under https://www.cde.ca.gov/ta/ac/cm/ (e.g. https://www.cde.ca.gov/ta/ac/cm/chronicdatafiles.asp and its layout https://www.cde.ca.gov/ta/ac/cm/chronic24.asp) | Dashboard indicator data behind the color bands: for each school, district and the state, the current and prior year figure for each state indicator, by student group, with the band CDE assigned | Browser download, annual, **one file per indicator** rather than one file | **Surveyed 2026-09-05; not acquired, nothing published.** D4 is not a file, it is nine: the 2025 Dashboard publishes eight state-indicator files (ELA, Math, Science, Chronic Absenteeism, Suspension, Graduation, College/Career, English Learner Progress) plus a Growth Model file -- 153,581,873 bytes and 1,104,219 data rows together -- and they do not share a schema. Column counts run 17 to 109; only 22 columns are common to all eight indicator files; College/Career carries 109, Growth carries no `indicator` column and adds `subject`, Suspension adds `type`, and ELA/Math/Science add their own participation-rate columns. Chronic Absenteeism spells the column `changeLevel` where all seven others spell it `changelevel`, so a parser matching one spelling silently drops the column on the other file. Two sentinel shapes are new here and neither is D2's and D3's `*`, of which these files contain zero: **suppression is a blank cell** (37,763 of the 114,225 chronic rows carry a blank `currnumer` and `currstatus` while `currdenom` is populated), and **`0` is a sentinel, not a value**, in `statuslevel`, `changelevel`, `color` and `box`, where CDE's layout defines it as "No Status Level" / "No Color" (55,651 of those same 114,225 rows read `color` = 0). `rtype` carries CDE's own `D` (district) and `X` (state) aggregate rows, so context can be read rather than summed. 21 distinct `studentgroup` codes appear across the nine files -- a third vocabulary, sharing codes with neither D2's (`RE_A`) nor D3's (`RA`) -- from 2 in the ELPI file to 20 in ELA, Math and Science, and all 21 would need reviewed display names. School coverage is not uniform either: 2,551 schools in the College/Career and Graduation files against 9,969 in Suspension, 9,971 distinct schools across all nine. **What is undecided is not the schema but whether any of it may be published:** `color`, `statuslevel`, `changelevel` and `box` are an ordered, state-assigned performance band (1=Red through 5=Blue; `box` is a position in a 5x5 grid), and rendering one is rendering an ordering of schools, which is what ADR 0002 refuses. The plain figures (`currstatus`, `currnumer`, `currdenom`, `change`) are copyable cells and carry no such problem. See docs/ROADMAP.md "M5 source survey" |
| D5 | Teacher Assignment Monitoring Outcome files, https://www.cde.ca.gov/ds/ad/filestamo.asp, linking https://www3.cde.ca.gov/demo-downloads/tamo/tamo2324.txt (file structure: https://www.cde.ca.gov/ds/ad/fstamo.asp). CDE publishes these from the Commission on Teacher Credentialing's CalSAAS system | By school: teaching FTE (full-time-equivalent, not integer counts) held on a clear credential and appropriately assigned, and the other authorization states the state tracks, with CDE small-cell masking where it applies | Browser download, annual. Record which school year's file was taken; it reports on a different cycle than D2 | **Acquired and schema-verified 2026-08-21; published on the school pages since 2026-09-05 (ADR 0005).** The 2023-24 file (`tamo2324.txt`, 234,206,408 bytes, 1,528,796 data rows) was downloaded and its header read directly, which found the provisional contract this project carried before acquisition (issue #5) wrong in every particular: real column names are spaced (`Total FTE`, `Clear FTE (count)`, ...) rather than concatenated; there are seven outcomes, not five (`incomplete` and `na` were missing entirely); values are fractional FTE, not integer assignment counts; `Charter School`/`DASS` are the words `All`/`Yes`/`No`, not D2's single-letter `ALL`/`Y`/`N`; the file's grain is one row per (school, charter, DASS, grade span, teacher experience level, teacher credential level, subject area) -- up to 150 rows per school, not one -- of which exactly one row per school (experience = credential = `ALL`, subject = `TA`) is CDE's own whole-school total, verified against all 10,064 schools in the file; and the real header carries CDE's own typo, `Unknown FTE FTE (percent)`. Scanning every numeric cell in the file (1,528,796 rows) found zero instances of `*` or any other unrecognized sentinel: unlike D2 and D3, this file does not appear to mask at all in this vintage, and its file-structure page carries no small-cell suppression rule the way D3's does. `src/homeroom/assignments.py` and `fixtures/tamo.sample.txt` were rewritten to match; `docs/ROADMAP.md` D5a records the measured counts. Publishing was deliberately held open as a separate decision this acquisition did not make, and this row read "`make data` and `make site` are unchanged and no D5 number about a real school is published anywhere" from 2026-08-21 until 2026-09-05. That was the honest record of the old state and is no longer true. The owner decided on 2026-09-05 to publish D5 (`docs/adr/0005-publish-teacher-assignment-monitoring.md`, answering issue #59), and it is now wired into `make data`, `make site` and `make publish` (Makefile `--assignments`) and rendered on every school page: the whole-school total FTE, the seven outcome FTE counts and the seven outcome shares, every one a copied cell, beside CDE's own district and statewide rows (`homeroom.context.load_assignment_context`, which takes only the row where charter status, DASS, grade span, experience level, credential level and subject area all read their aggregated value) and with coverage stated. No share is divided out of a count and no total is summed from the file's other rows. The ask layer (ADR 0003) does not carry D5 and its evidence bundle is unchanged |
| D6 | ESSA Per-Pupil Expenditure (PPE) data files, https://www.cde.ca.gov/fg/ac/es/essappedata.asp, linking https://www.cde.ca.gov/fg/ac/es/documents/essappe2425data.xlsx | Per-pupil expenditure for each school and each LEA, split federal against state and local, and school-site against central | Browser download, annual, **XLSX only** -- no TXT or CSV is published | **Surveyed 2026-09-05; not acquired, nothing published.** The pointer this row carried until 2026-09-05, "Per-pupil expenditure (SACS/LCFF public files)", was aimed at the wrong files, and both of them are LEA-level: the SACS Annual Financial Data (https://www.cde.ca.gov/ds/fd/fd/) ships as self-extracting Windows `.exe` archives meant for Microsoft Access, and the Current Expense of Education (https://www.cde.ca.gov/ds/fd/ec/) states on its own page that it "is calculated at a school district level". The SARC file that looks like the school-level one is not: https://www3.cde.ca.gov/researchfiles/sarc/sarc2425/expend.txt (662,397 bytes, 10,274 rows, one per school) has a `STEXP` column holding exactly one distinct value, 11146.18, on all 10,274 rows -- the statewide figure, not the school's -- and CDE's note on that download page says why: "The CDE provides State Expenditures Per Pupil (Unrestricted), and District and State Average Teacher Salary data. The remaining data is to be provided by the LEA." The source that does publish a school figure is ESSA PPE, required by ESEA sections 1111(h)(1)(C)(x) and 1111(h)(2)(C). The 2024-25 workbook (`essappe2425data.xlsx`, 1,035,042 bytes, prepared June 2026) carries two sheets: "ESSA School Data", header on row 7, 10,065 school rows over 11 columns, every CDS code 14 characters and none duplicated; and "ESSA LEA Data", 1,980 rows on a different 7-column layout, which is CDE's own LEA aggregate and so the district context row, never a sum of schools. Its sentinel is `DNR` ("Did Not Report"), not `*`: 187 of the 10,065 school rows, and always all four expenditure columns together, never a subset. Genuine zeros are separately present and common (1,352, 842, 817 and 140 across the four columns), so zero-versus-withheld is live in this file rather than theoretical. **The complement rule is the whole design problem here:** the file publishes four per-pupil components -- School-Federal, School-State & Local, Central-Federal, Central-State & Local -- and no total, so a single "per-pupil spending" figure would have to be summed rather than copied, and on a `DNR` row a sum would read a withheld component as zero. The denominator is not one thing either: `Student Membership Type` is Census Day Enrollment on 9,767 rows, Cumulative Enrollment on 203 and Annual ADA on 95, so two schools' figures are not necessarily per the same count. The LEA sheet is not column-comparable to the school sheet: it carries an `Expenditures-Excluded ($)` column with no school-sheet counterpart. One upstream typo was found, Achieve Academy (CDS 01612590111476) filed under "Calaveras" on the LEA sheet against "Alameda" on the school sheet and an `01` = Alameda CDS prefix, 1 of the 986 LEA rows whose CDS also appears on the school sheet; D1 is the spine and names are display-only, so the existing rule already covers it. See docs/ROADMAP.md "M5 source survey" |

Rules:

- CDS code (14-digit county-district-school) is the only join key. Names are display-only.
- CDE masks small cells (published as `*`). A masked cell is `null` in every artifact and
  is counted in coverage output. It is never zero.
- No third-party or commercial data (no ratings sites, no real-estate feeds). If CDE does
  not publish it, Homeroom does not show it.
- A parser may be built ahead of its file, but a number may not. A source whose status
  reads "awaiting acquisition" has an access date of `null` everywhere, publishes nothing,
  and says so in `coverage.json`; the pipeline is tested to keep the code constant and this
  table in agreement, so a build cannot stamp a date nobody recorded.
- Each source keeps its own school year. Sources report on different cycles, and a profile
  carries each year beside its own data rather than one label over data from two calendars.
- *Surveyed* is weaker than *acquired* and the table means the difference. A surveyed source
  has had its real files opened and their schema measured, and that measurement is recorded
  here so the next parser is not written against a guess -- the D5 lesson, where a provisional
  contract turned out wrong in every particular. It has not entered the pipeline: the files are
  not in `data/raw/`, no access date is recorded anywhere, and no number from them is published.
  The D4 and D6 surveys dated 2026-09-05 were taken by a scripted HTTPS GET carrying a browser
  `User-Agent`, because `www3.cde.ca.gov` answers a non-browser client with a firewall redirect
  instead of the file. That is not the documented browser acquisition step, and it is recorded
  as a survey rather than an acquisition for exactly that reason; acquiring either source is
  still a person's step.

## Documentation corpus (ADR 0003)

The ask layer quotes CDE's own definitions rather than paraphrasing them. The
pages it quotes are retrieved by `tools/corpus_fetch.py` into `corpus/`, and
`corpus/manifest.json` is their provenance register: URL, retrieval date (UTC),
the page's own "Last Reviewed" date, SHA-256 of the HTML as received and of the
text as committed, and passage count. The same rules apply as to the data files:
a person runs the fetch, CI never does, and a file that no longer matches its
recorded hash is refused by the loader rather than quoted. See `corpus/README.md`.
