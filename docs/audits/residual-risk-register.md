# Residual-risk register — homeroom

<!-- Companion to docs/audits/threat-model.md (RESPONSIBLE-TECH-FRAMEWORK.md §F).
     Every threat-model row whose mitigation is incomplete lands here with an
     owner and a decision. Review each open row at least quarterly. -->

- **Date of last review:** 2026-08-08
- **Register owner:** Chelsea Kelly-Reif

## Register

| ID | Risk description | Likelihood | Impact | Owner | Mitigation in place | Decision | Review date |
|---|---|---|---|---|---|---|---|
| RR-01 | An acquired file is authentic and correctly shaped but is the wrong file: a prior year, a different aggregation, or a page CDE has since renamed. The pipeline would parse it cleanly and publish it under this year's label | Medium | High | Chelsea Kelly-Reif | PROVENANCE.md records the school year, access date, byte count and row count of every file; artifacts stamp the academic year; the site build refuses to render when the context year and the profile year disagree. None of that catches a wrong file that is internally consistent | Accept — the acquisition step is a person reading a page, and no automated control can substitute for that without fetching, which this project deliberately does not do. Mitigated by recording enough that a wrong file is discoverable after the fact | 2026-11-08 |
| RR-02 | A transitive Python or npm dependency is compromised between audit runs and alters rendered output | Low | High | Chelsea Kelly-Reif | `uv.lock` committed, installed `--frozen`; every action SHA-pinned and gated by zizmor; pip-audit and npm audit in `make verify`; deterministic build means altered output changes bytes | Track — no Dependabot `minimumReleaseAge` configured in this repo yet, and no SBOM is produced outside the release path. Revisit when the first release is cut | 2026-11-08 |
| RR-03 | CDE's own masking is imperfect for some cell, and Homeroom republishes a figure that is re-identifying despite behaving correctly | Low | Medium | Chelsea Kelly-Reif | Homeroom publishes only what CDE published, performs no arithmetic across cells, and derives no value from complements. The suppression decision is CDE's and is not second-guessed | Accept — republishing an already-public aggregate at the same grain adds no disclosure. Would need revisiting if Homeroom ever joined two sources in a way that narrowed a cell | 2026-11-08 |
| RR-04 | `.github/signing-allowed-signers` still holds the scaffold placeholder, so the release path's signed-tag verification cannot succeed | High | Low | Chelsea Kelly-Reif | `release.yml` fails closed: with a placeholder present no tag verifies, so no release can be published rather than an unsigned one slipping out | Track — blocking for the first tagged release only. Needs the maintainer's real SSH signing principal and public key, which cannot be supplied by tooling | 2026-11-08 |
| RR-05 | The keyboard and screen-reader walkthrough in each language, and the narrow-screen reflow check, have not been performed. Automated gates cover the rule sets a headless DOM can decide and nothing else | Medium | Medium | Chelsea Kelly-Reif | html-validate plus axe-core over WCAG 2.0/2.1/2.2 A and AA on every page in both languages; contrast measured in pytest against both palettes; structure, landmarks, scoped headers, skip link and reachable scroll regions asserted. jsdom paints nothing, so `color-contrast` and `target-size` are excluded there and named in `tools/a11y.mjs` | Track — `docs/RESPONSIBLE-TECH-AUDITS.md` §E stays open until a person walks a built page in each language. No gate can close this one | 2026-11-08 |
| RR-06 | The Spanish pages have been reviewed only by their author. The parity gate proves both locales exist, share keys and placeholders, and differ in wording; it cannot judge whether the Spanish is good | Medium | Medium | Chelsea Kelly-Reif | Parity enforced in `tests/test_i18n.py`; CDE-published English names marked `lang="en"` on Spanish pages so a screen reader does not read them with Spanish phonemes; CONTRIBUTING.md asks for review by a second Spanish speaker | Track — open until a second speaker reviews. Spanish is a launch requirement, not a translation layer, so this is a quality gap rather than a missing feature | 2026-11-08 |
